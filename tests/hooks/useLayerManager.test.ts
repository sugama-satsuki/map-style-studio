// useLayerManager は未実装のスタブです。実装後にテストを追加してください。
describe('useLayerManager', () => {
  it('モジュールがインポートできる', async () => {
    const mod = await import('../../src/hooks/useLayerManager');
    expect(mod).toBeDefined();
  });
});
