// useProjectStorage は未実装のスタブです。実装後にテストを追加してください。
describe('useProjectStorage', () => {
  it('モジュールがインポートできる', async () => {
    const mod = await import('../../src/hooks/useProjectStorage');
    expect(mod).toBeDefined();
  });
});
