// useFileIO は未実装のスタブです。実装後にテストを追加してください。
describe('useFileIO', () => {
  it('モジュールがインポートできる', async () => {
    const mod = await import('../../src/hooks/useFileIO');
    expect(mod).toBeDefined();
  });
});
