// useZoomControl は未実装のスタブです。実装後にテストを追加してください。
describe('useZoomControl', () => {
  it('モジュールがインポートできる', async () => {
    const mod = await import('../../src/hooks/useZoomControl');
    expect(mod).toBeDefined();
  });
});
